<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    style="@style/Screen"
    >
    <LinearLayout
        android:id="@id/mainInfoPanel"
        style="@style/MainInfoPanel"
        >
        <TextView
            android:id="@id/mainInfoPanelText"
            android:text="@string/main_info_panel_text_1"
            style="@style/InfoText"
            />
        <Button
            android:id="@id/buttonSimplenoteSettings"
            android:text="@string/sign_in"
            style="@style/InfoButton"
            />
        <TextView
            android:id="@id/mainInfoPanelText"
            android:text="@string/main_info_panel_text_2"
            style="@style/InfoText"
            />
        <Button
            android:id="@id/buttonSimplenote"
            android:text="@string/create_account"
            style="@style/InfoButton"
            />
    </LinearLayout>
    <ListView
        android:id="@+android:id/list"
        style="@style/NoteList"
        >
    </ListView>
</LinearLayout>

